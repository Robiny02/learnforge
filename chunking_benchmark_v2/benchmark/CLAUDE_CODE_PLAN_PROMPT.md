你现在先不要写代码，也不要直接修改项目文件。

我准备了一个 RAG Chunking Benchmark 数据集，需要你先做 planning。请基于当前目录真实情况和少量样本内容，规划如何比较不同切片策略。

数据目录可能包括：

```text
benchmark/data/raw/github_md/
benchmark/data/raw/xiaohongshu_interview/
benchmark/data/raw/xiaolin_blog/
benchmark/data/eval_queries/
```

请重点注意：小红书数据不是完全随机噪声，因为它是按 keyword 和时间抓取的，所以大部分内容仍然和求职、实习、面经相关。噪声更可能来自：

1. 非计算机岗位面经，例如产品、运营、HR、销售、管培生等；
2. 个人感受、求好运、timeline、offer 玄学；
3. 过短帖子，只说“拷打项目”“手撕没写出来”，没有完整题目；
4. 评论区补充和楼主更新；
5. 图片 OCR 中有面试题截图，也可能有低相关的个人感受文字。

请你先完成 plan，不要执行实现：

1. 检查目录结构和现有文件。
2. 阅读每类数据的 2～3 个样本，分析真实噪声类型。
3. 设计数据加载方式。
4. 设计 OCR / block 相关性判断策略，重点说明：
   - 哪些 OCR 应该保留；
   - 哪些 OCR 应该隔离；
   - 哪些不应该直接删除；
   - 是否需要轻量模型辅助判断。
5. 设计四种切片策略的公平比较方法：
   - 固定长度切片；
   - 结构切片 + 父子切片；
   - 语义切片；
   - LlamaIndex 或类似框架切片。
6. 设计评测指标：
   - chunk 统计；
   - 结构完整性；
   - OCR 相关性；
   - 噪声污染率；
   - 检索 Recall / Precision / MRR；
   - 父子 chunk 是否应该分开评测。
7. 给出分阶段执行计划：
   - Phase 1：最小可运行切片 + 统计；
   - Phase 2：加入轻量检索评测；
   - Phase 3：加入 OCR/block 相关性判断；
   - Phase 4：接入真实本地图片和更多爬虫数据。
8. 说明推荐文件结构、输入输出格式、运行方式和风险。

现在只输出 plan，不要写代码，不要创建文件。
